# Welcome to Your Eternal Context Engine Vault

**The Graph is the Interface** — just like the 3D frontend demo.

Open this folder in Obsidian (File → Open Vault → choose the `vault/` folder).

Then press **Ctrl/Cmd + G** for Graph View. You will see a beautiful, immediately connected knowledge graph from the demo notes below (wikilinks + tags + properties). Color-coded folders, premium callouts, folder-colored sidebar — the full "beautiful Obsidian interface we had before", ready for your content.

- The engine keeps everything indexed automatically (dense + BM25 + optional multi-vector MaxSim on power hardware).
- All plain Markdown. Git friendly. Works everywhere.
- Your AIs (Claude, Grok, Cursor, etc.) reach it via MCP.

## The Initial Graph (demo nodes — already linked)
- [[Dashboard]] — system status + quick links
- [[Core Concepts]] — how ECE works
- [[Hardware Profile]] — processor classes + gates (constrained → HIGH)
- [[Multi-Vector Assimilation]] — per-token late interaction (MaxSim) + why it assimilates fine details
- [[Automatic Upgrades]] — multi-vector population happens automatically on capable machines (no manual reindex)

Start here: fill `Core/USER.md`, `Core/SOUL.md`, `Core/COMPANY-SOUL.md`.

Write anywhere. Every new note with a [[wikilink]] or #tag makes the graph richer and more useful.

The graph view watching your memory grow over time is the whole point.

#ece #graph-first #memory #template
